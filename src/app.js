"use strict";
var splide = new Splide( '.splide', {
    perPage: 3,
    rewind : true,
  } );
  
  splide.mount();
  
splide.mount();
new Splide(".splide", {
  type: "loop",
  autoplay: "pause",
  perPage: 4,
});

